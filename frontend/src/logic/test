import validator from '../logic/PrerequisiteValidation'

function convertToProposition(rawPrereqNotes):


function validate(proposition, seasonIndex, plan):
  // grab courses before seasonIndex. create a list
  const previousCourses = getPreviousCourses(seasonIndex, plan)

  // determine value of proposition based on this list
  return validateProposition(proposition, getPreviousCourses(seasonIndex, plan))


const validProposition = convertToProposition(course.prereqNotes)
const isValidPlacement = validate(validProposition, seasonIndex, plan);


this may not be correct. check again with topological sort and how it works.

instead of specific course, plan-wide. this type of singular checking
will lead to some type of conflicts